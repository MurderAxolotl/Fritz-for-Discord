services:
  fritz:
    restart: unless-stopped
    build: .
    volumes:
      - ./config:/fritz/config
      - ./plugins:/plugins
      - ./cache:/cache
      - ./logs:/fritz/logs
      - ./ptk:/ptk
